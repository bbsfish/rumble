put_introducetext() {
    let request = XMLHttpRequest();
    request.open("GET", "./assets_docs.txt", true);
    request.onreadystatechange = function () {
        if (request.readyState == 4) {
            
        }
    }
}
